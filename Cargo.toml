[package]
name = "plato-backprop"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "Backpropagation-through-prompt tracking for PLATO nano models"
repository = "https://github.com/SuperInstance/plato-backprop"

[dependencies]
serde = { version = "1", features = ["derive"] }
uuid = { version = "1", features = ["v4", "serde"] }
